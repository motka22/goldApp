import { MyhightlightDirective } from './myhightlight.directive';

describe('MyhightlightDirective', () => {
  it('should create an instance', () => {
    const directive = new MyhightlightDirective();
    expect(directive).toBeTruthy();
  });
});
